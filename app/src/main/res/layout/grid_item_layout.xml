<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dice_grid_layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="5dp"
    android:background="@drawable/grid_color_selector"
    android:orientation="vertical"
    android:padding="5dp" >

    <ImageView
        android:id="@+id/dice_image"
        android:layout_width="100dp"
        android:layout_height="100dp" />

    <TextView
        android:id="@+id/dice_type_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/dice_image"
        android:layout_alignTop="@id/dice_image"
        android:layout_alignRight="@id/dice_image"
        android:layout_alignBottom="@id/dice_image"
        android:layout_margin="1dp"
        android:textSize="12sp"
        android:gravity="center" />

</RelativeLayout>